export const DEPARTMENTS = ["AI & Development", "CS", "Math", "English"];

export const DEPARTMENT_OPTIONS = DEPARTMENTS.map((dept) => ({
  value: dept,
  label: dept,
}));

export { MOCK_SUBJECTS } from "./mock-data";
